sudo apt update

boost, use system install
sudo apt install libboost-all-dev

dlib, header only
sudo apt install libdlib-dev

ffmpeg with codecs
sudo add-apt-repository multiverse
sudo add-apt-repository restricted
sudo apt update
ffmpeg:
sudo apt install \
  ffmpeg \
  libavcodec-dev \
  libavformat-dev \
  libavutil-dev \
  libswscale-dev \
  libswresample-dev
codecs:
sudo apt install \
  libx264-dev \
  libx265-dev
  
freetype
sudo apt install libfreetype6-dev

gl related
glew:
sudo apt install libglew-dev
opengl libs
sudo apt install \
  libgl1-mesa-dev \
  libglu1-mesa-dev
x11 + gl context requirements
sudo apt install \
  xorg-dev \
  libxinerama-dev \
  libxcursor-dev \
  libxi-dev \
  libxrandr-dev
glm:
sudo apt install libglm-dev

hdf5
sudo apt install \
  libhdf5-dev \
  libhdf5-cpp-103

jdk
sudo apt install openjdk-17-jdk

openblas
sudo apt install libopenblas-dev

opencl
sudo apt install \
  ocl-icd-libopencl1 \
  ocl-icd-opencl-dev \
  opencl-headers \
  clinfo
sudo apt install nvidia-opencl-icd
it may fail and ask for a version matching the driver
check driver version:
nvidia-smi
install with version:
sudo apt install libnvidia-compute-580

opencv
sudo apt install libopencv-dev

openjpeg
sudo apt install libopenjp2-7-dev

openvr
sudo apt install steam

openxr
sudo apt install \
  libopenxr-loader1 \
  libopenxr-dev
runtime:
steam or
mondado:
sudo apt install monado-runtime

python
sudo apt install python3 python3-dev python3-venv python3-distutils

teem
sudo apt install libteem-dev

zlib
sudo apt install zlib1g-dev

wxwidgets/gtk
sudo apt install libgtk-3-dev
sudo apt install pkg-config

png
sudo apt install libpng-dev
